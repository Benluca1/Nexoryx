"""Nexoryx — hochmodulares, lokal + cloudfähiges Multi-Agenten-Framework.

Dieses Paket ist bewusst so gebaut, dass der Kern (Hardware-Erkennung, Profil,
Config, CLI `doctor`) ohne externe Abhängigkeiten läuft. Schwergewichtige Teile
(Inferenz, Router, Agenten) werden in späteren Phasen als optionale Extras
ergänzt — siehe plan / README.
"""

__version__ = "0.0.1"
